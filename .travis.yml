language: php
php:
  - 5.3
  - 5.4
before_script:
  - mysql -e 'create database buckutt_test;'
  - curl -s https://getcomposer.org/installer | php
  - php composer.phar install --dev
  - cp config.inc.dist.php config.inc.php
  - cd tests/
script: ./phpunit --coverage-text *.test.php

